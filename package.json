{
  "name": "happymusic",
  "version": "1.0.0",
  "private": true,
  "description": "HappyMusic - app Android nativo (Capacitor)",
  "scripts": {
    "build:native": "esbuild src/capacitor-entry.js --bundle --format=iife --outfile=www/js/capacitor-bundle.js --minify",
    "sync": "npm run build:native && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "6.2.1",
    "@capacitor/cli": "6.2.1",
    "@capacitor/core": "6.2.1",
    "@capacitor/filesystem": "6.0.4",
    "@jofr/capacitor-media-session": "4.0.0",
    "esbuild": "^0.28.1"
  }
}
